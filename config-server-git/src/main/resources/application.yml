server:
  port: 8889

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/emreustunn/Java6ConfigServer.git
          default-label: main
          skip-ssl-validation: true
          username: emreustunn
          password: ${java6configpassword}